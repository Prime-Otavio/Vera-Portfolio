node_modules/
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example
# .env.production contem apenas valores publicos (URL + anon key) para o build
# na Vercel/Railway. A chave secreta da Anthropic nunca entra aqui.
!.env.production

# Supabase
supabase/.branches
supabase/.temp
.vercel
